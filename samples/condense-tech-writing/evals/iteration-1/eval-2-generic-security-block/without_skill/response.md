## Security

Presigned download URLs are generated by `/api/download` after the Stripe webhook confirms payment. Each URL is bound to a specific clip ID and customer email, and expires after 1 hour. That TTL balances two constraints: slow connections need enough time to complete a download, and a leaked URL should not grant indefinite access.
